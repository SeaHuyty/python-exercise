# range() Function:
numbers = range(5, 10, 2) # The first is the elements and the second is the end value and the third is the skip.
for number in numbers:
    print(number)
for number in range(5):
    print(number) # It'll display the range(5) which the number starts from zero.