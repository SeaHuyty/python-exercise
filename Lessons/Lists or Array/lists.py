# Lists: Maybe it's the same as array in C.
names = ["Noaca", "Shaid", "Ficaf"] # It's to remember that computer number start from zero.
print(names[0])
print(names[-1]) # If it's negative then it'll start counting backward.
# If we want to change the spelling of a name in that list.
names[0] = "Noacas"
print(names)
# Or we want to pick two names.
print(names[0:2]) # It excludes the last one.