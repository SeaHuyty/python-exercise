# Turples: it's like list but once we create them, we cannot change them.
numbers = (1, 2, 3, 3) # We use parenthesis to define a turple. Bracket[] to defines a list.
print(numbers.count(3)) # We use count to count the amount of an element.